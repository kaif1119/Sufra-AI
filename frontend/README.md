# Sufra-AI Frontend

React, Vite, Tailwind CSS, and Redux Toolkit frontend for the Sufra-AI chat app.

## Scripts

- `npm run dev` starts the Vite dev server.
- `npm run build` creates a production build.
- `npm run lint` checks frontend source files.
- `npm run preview` serves the production build locally.
