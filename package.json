{
  "name": "sufra-ai",
  "private": true,
  "version": "1.0.0",
  "description": "A full-stack AI chat application with Google OAuth, JWT authentication, MongoDB chat history, and real-time Mistral AI response streaming.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm ci --prefix backend && npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "docker compose up --build"
  }
}
